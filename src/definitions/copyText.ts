const buttons = {
    search: "Search",
    create: "Create",
    edit: "Edit"
}

const errors = {
    noUsersFound: "No users found"
}

const placeholders = {
    search: "Enter a user's name"
}

export const copy = {buttons, errors, placeholders}